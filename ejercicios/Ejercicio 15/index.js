let nombre = "Miguel",
  apellido = "Sanchez";
const misDatos = {
  nombre,
  apellido,
};

/* sessionStorage.setItem("misDatos", JSON.stringify(misDatos));
console.log(JSON.parse(sessionStorage.getItem("misDatos", misDatos)));

localStorage.setItem("misDatos", JSON.stringify(misDatos));
console.log(JSON.parse(localStorage.getItem("misDatos", misDatos)));

document.cookie =
  `misDatos=${JSON.stringify(misDatos)}; expires=` +
  new Date(Date.now() + 60 * 100).toUTCString();
console.log(document.cookie); */
